<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network security config (app/src/debug — merged in for debug
  builds only, never release). Allows plain HTTP to the Android emulator's
  alias for the host machine's localhost (see build.gradle.kts's debug
  DEFAULT_BASE_URL, http://10.0.2.2:5000/) and to a real device's usual
  "same Wi-Fi as my laptop" dev-server addresses, without loosening
  cleartext policy for anything else — release still blocks all cleartext
  traffic via the platform default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
