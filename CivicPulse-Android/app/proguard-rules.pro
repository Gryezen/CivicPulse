# Add project specific ProGuard rules here.
# Retrofit / OkHttp / kotlinx.serialization all ship consumer rules, so this
# file is intentionally minimal for now.
-keepattributes Signature
-keepattributes *Annotation*
