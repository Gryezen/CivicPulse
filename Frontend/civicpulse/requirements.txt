flask==3.1.3
gunicorn==23.0.0
flask-sqlalchemy==3.1.1
flask-login==0.6.3
psycopg2-binary==2.9.10
python-dotenv==1.0.1

# classify.py v2 — TF-IDF + Logistic Regression classifier bundle, trained
# offline by train_classifier.py. See classify.py's docstring: the app
# still runs and falls back to keyword rules if these aren't installed or
# classifier_model.joblib is missing.
scikit-learn==1.5.2
joblib==1.4.2

# PolicyGyaan bridge (policy_engine.py) — optional at runtime: if
# GOOGLE_API_KEY isn't set, PolicyRecommender falls back to keyword scoring
# and this package is never touched.
google-genai==1.62.0

# policy_ingest.py's URL adapter (fetch_from_url) — only touched if you
# actually run a sync against an http(s) source; the file adapter
# (fetch_from_file) needs nothing extra.
requests==2.32.3
